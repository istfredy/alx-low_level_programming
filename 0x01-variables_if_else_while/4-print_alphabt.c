#include <stdio.h>
/**
 * main - Entry of point
 *
 * Return: 0 on success
 */
int main(void)
{
/* My Code ON */
char c = 'a';

while (c <= 'z')
{
if (c != 'e' && c != 'q')
{
putchar(c);
c++
}
}
putchar('\n');
return (0);
}
